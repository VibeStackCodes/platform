// Placeholder — overwritten by `bun run build` (server bundle step).
// Exists in git so Vercel detects the serverless function entry point.
export default (req) => new Response('Build not run', { status: 503 })

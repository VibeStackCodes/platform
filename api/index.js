// Placeholder — overwritten by `node scripts/bundle-server.mjs` during build.
// This file must exist in git so Vercel's `functions` config can match it.
module.exports.default = (req) => new Response('Build step did not run', { status: 500 })

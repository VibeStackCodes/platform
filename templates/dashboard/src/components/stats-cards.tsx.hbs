import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Activity, Users, BarChart3, TrendingUp } from 'lucide-react';

const stats = [
  { title: 'Total Users', value: '0', icon: Users, change: '+0%' },
  { title: 'Active Now', value: '0', icon: Activity, change: '+0%' },
  { title: 'Revenue', value: '$0', icon: BarChart3, change: '+0%' },
  { title: 'Growth', value: '0%', icon: TrendingUp, change: '+0%' },
];

export function StatsCards() {
  return (
    <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
      {stats.map((stat) => (
        <Card key={stat.title}>
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium">{stat.title}</CardTitle>
            <stat.icon className="h-4 w-4 text-[hsl(var(--muted-foreground))]" />
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold">{stat.value}</div>
            <p className="text-xs text-[hsl(var(--muted-foreground))]">{stat.change} from last month</p>
          </CardContent>
        </Card>
      ))}
    </div>
  );
}
